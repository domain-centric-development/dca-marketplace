package {{basePackage}}.{{context}}.application.{{usecasename}};

/**
 * Command input for the {{Name}} use case.
 *
 * <p>Commands carry the data needed to mutate state. They are immutable records.
 */
public record {{Name}}Command(
{{commandFields}}
) {}
