namespace {{rootNamespace}}.{{Context}}.Application.{{FeatureSegment}}{{Name}};

/// <summary>Query input for the {{Name}} use case — the criteria for a read. Immutable record.</summary>
public sealed record {{Name}}Query(
{{queryFields}}
);
