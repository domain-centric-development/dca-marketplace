namespace {{rootNamespace}}.{{Context}}.Application.{{FeatureSegment}}{{Name}};

/// <summary>Command input for the {{Name}} use case — the data needed to mutate state. Immutable record.</summary>
public sealed record {{Name}}Command(
{{commandFields}}
);
