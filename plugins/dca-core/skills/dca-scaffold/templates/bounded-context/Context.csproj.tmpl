<!-- Bounded context: {{Context}}. Generated by dca-scaffold; add it to the solution (dotnet sln add). -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>{{rootNamespace}}.{{Context}}</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../{{rootNamespace}}.SharedKernel/{{rootNamespace}}.SharedKernel.csproj" />
  </ItemGroup>
  <!-- DomainCentric.BuildingBlocks comes from the shared kernel project (or add the PackageReference here). -->
</Project>
