# {{PRODUCT_NAME}} — technical decisions

<!-- The technical description: the decisions a change may not take in passing, written with the
person who owns them. Decisions and their reason, never a design — no packages, classes or
endpoints. The headings stay as they are; the text is in the project's language. A heading with
nothing to decide gets one honest line ("no integrations"), never a placeholder. These comments do
not count as content. -->

## Stack

<!-- Language, framework and build tool, with the reason where it is not obvious. -->

## Frontend approach

<!-- Server-rendered pages, a client application, none — and what that excludes (a client
framework on server-rendered pages, for example). -->

## Persistence

<!-- Where state is kept and how: one database, files, none. A second persistence is a decision,
so name what the product uses and what it does not. -->

## Runtime

<!-- Where and how it runs: a container, a platform, a desktop, a device — and what it needs there. -->

## Integrations

<!-- The external systems the product talks to, with the direction, what is sent and received — each field with
its type, a money amount as a decimal string, never a JSON number — and what counts as unavailable. A contract left
open here is filled differently by every implementation. -->

## Version policy

<!-- How dependencies are chosen and kept current: from the generator, pinned, the latest release. -->
