# store-with-in-memory-adapter

- [Store skeleton (output port + in-memory outgoing adapter) — Java](java.md) — The Java code of Store skeleton (output port + in-memory outgoing adapter). The prose, the evidence and what
