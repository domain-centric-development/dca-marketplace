# event-driven-architecture-in-spring-modulith

- [Event-Driven Architecture in Spring Modulith — Anti-Corruption Layer (ACL) Pattern](anti-corruption-layer-acl-pattern.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Best Practices](best-practices.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Domain Events vs Integration Events](domain-events-vs-integration-events.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Event Architecture Comparison](event-architecture-comparison.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Event Consumption Pattern](event-consumption-pattern.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Event Mapper Pattern (Domain Event → Integration Event)](event-mapper-pattern-domain-event-integration-event.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Event Publishing Pattern](event-publishing-pattern.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Idempotent Consumers](idempotent-consumers.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Event-Driven Architecture in Spring Modulith — Spring Modulith Event Publication Registry](spring-modulith-event-publication-registry.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
