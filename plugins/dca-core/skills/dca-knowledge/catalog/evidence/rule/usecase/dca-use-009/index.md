# dca-use-009

- [Use cases that save an aggregate must publish its domain events — C# expression](c-expression.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — C# helper EventFreeAggregate](c-helper-eventfreeaggregate.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — C# helper IntraClassCalls](c-helper-intraclasscalls.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `calls`](calls.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `EventFreeAggregate.aggregate`](eventfreeaggregate-aggregate.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `EventFreeAggregate.noRegistration`](eventfreeaggregate-noregistration.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `EventFreeAggregate.platform`](eventfreeaggregate-platform.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `EventFreeAggregate.repository`](eventfreeaggregate-repository.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `IntraClassCalls.callersOf`](intraclasscalls-callersof.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `IntraClassCalls.closure`](intraclasscalls-closure.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `IntraClassCalls.entryPointsOf`](intraclasscalls-entrypointsof.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `IntraClassCalls.isEntryPoint`](intraclasscalls-isentrypoint.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `IntraClassCalls.reachableFrom`](intraclasscalls-reachablefrom.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — Overview](overview.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `pathName`](pathname.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Use cases that save an aggregate must publish its domain events — `publishAfterSaving`](publishaftersaving.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
