# dca-use-013

- [Declaratively transactional use cases must not call remote-capable output ports — `AnnotationRoles.isMetaAnnotatedWithAny`](annotationroles-ismetaannotatedwithany.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — C# expression](c-expression.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — C# helper IntraClassCalls](c-helper-intraclasscalls.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — C# helper OperationPolicy](c-helper-operationpolicy.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — `IntraClassCalls.callersOf`](intraclasscalls-callersof.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — `IntraClassCalls.closure`](intraclasscalls-closure.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — `isTransactional`](istransactional.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — `isTransactionalResource`](istransactionalresource.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — `notCallRemotePortsWhenTransactional`](notcallremoteportswhentransactional.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — Overview](overview.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Declaratively transactional use cases must not call remote-capable output ports — `useCases`](usecases.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
