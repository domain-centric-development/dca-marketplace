# dca-tac-016

- [Repositories must only exist for Aggregate Roots — `boundAggregateArgument`](boundaggregateargument.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Repositories must only exist for Aggregate Roots — C# expression](c-expression.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Repositories must only exist for Aggregate Roots — `classesMatching`](classesmatching.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Repositories must only exist for Aggregate Roots — `fail`](fail.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Repositories must only exist for Aggregate Roots — Overview](overview.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
- [Repositories must only exist for Aggregate Roots — `repositoryInterfaces`](repositoryinterfaces.md) — Full node and context. This is an evidence excerpt; retain the parent selection and caveats.
