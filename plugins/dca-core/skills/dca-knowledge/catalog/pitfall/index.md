# pitfall

- [Anemic domain model](anemic-domain-model.md) — Aggregates and entities reduced to bags of getters and setters, with all the business rules living in application-lay...
- [Authenticated is not authorized: a filter that enriches every request](authenticated-is-not-authorized.md) — An authentication filter or middleware that resolves an identity for *every* request — minting an anonymous one for a...
- [Authorization in the domain layer: aggregates that decide who may act](authorization-in-domain-layer.md) — An aggregate method takes the caller as a parameter and branches on their role or permissions — `order.cancel(user)` ...
- [Business logic in an adapter](business-logic-in-adapter.md) — A controller (or a repository implementation) that does more than translate and delegate — it validates a business ru...
- [Clearing domain events before they are dispatched](clearing-domain-events-before-dispatch.md) — A `DomainEventPublisher` implementation that snapshots the aggregate's events, calls `clearDomainEvents()`, and then ...
- [CSRF-exempt API that accepts cookie authentication](csrf-exempt-api-that-accepts-cookies.md) — The security configuration ignores CSRF for `/api/**` ("APIs are token-based") while the authentication filter still ...
- [Cyclic module dependency](cyclic-module-dependency.md) — Two bounded contexts that reference each other: Ordering depends on Shipping *and* Shipping depends on Ordering. Each...
- [Declarative transaction without a transaction manager](declarative-transaction-without-a-transaction-manager.md) — A Spring application in its in-memory phase — `spring-boot-starter`, `spring-modulith-starter-core`, repositories ove...
- [Default construction can bypass value validation](default-construction-bypasses-validation.md) — A validated constructor does not protect every entry: readonly structs have a default value, and deserialisation or
- [Adapter DTO in the application layer](dto-in-application-layer.md) — Passing a presentation or transport DTO — a REST request body, a JSON-bound `*Dto`, a form-backing object — straight ...
- [An event listener performs the external effect itself](event-listener-performs-the-external-effect.md) — A listener receives an integration event and calls the outside world in the same method: deserialise the message, bui...
- [Exposing domain objects over REST](exposing-domain-objects-over-rest.md) — A REST controller that returns an aggregate or entity directly — `return orderRepository.findById(id)` serialized str...
- [Framework leak in the domain layer](framework-leak-in-domain.md) — Domain classes that import Spring or JPA — `@Entity`, `@Component`, `@Service`, `@Autowired`, `@Table`, `jakarta.pers...
- [God port: one fat input port for many use cases](god-port.md) — A single wide input-port interface — `OrderService` with `placeOrder`, `cancelOrder`, `addItem`, `applyDiscount`, `re...
- [Modifying two aggregates in one transaction](modifying-two-aggregates-in-one-transaction.md) — A single use case that loads two aggregate roots, mutates both, and saves both inside one atomic transaction — `Place...
- [Operational module marked as a bounded context](operational-module-marked-as-a-bounded-context.md) — A cross-cutting module — an admin shell, a "common" module, a reporting surface that only reformats other contexts' d...
- [Outbox entry written after the commit](outbox-entry-written-after-commit.md) — An integration-event publisher that registers the outbox publication in an *after-commit* hook — "so a rolled-back us...
- [Publishing events one by one instead of publishAndClearEvents](publish-instead-of-publish-and-clear.md) — A use case that saves the aggregate and then dispatches its events by hand:
- [Publishing domain events without a transaction](publishing-domain-events-without-a-transaction.md) — A use case that saves and publishes but carries neither `@Transactional` nor an explicit `TransactionBoundary` block....
- [Raw cross-context import](raw-cross-context-import.md) — One bounded context reaching directly into another's internals — `import com.shop.ordering.domain.Order;` from inside...
- [Reconstitution raises the creation event](reconstitution-raises-creation-event.md) — A repository adapter — in-memory, JDBC or JPA — rebuilds a loaded aggregate through the same factory that creates a n...
- [Remote call inside a transaction](remote-call-inside-a-transaction.md) — A `@Transactional` use case (or the body of an explicit transaction block) that calls an output port which may leave ...
- [A Repository for something that is not an aggregate root](repository-for-non-aggregate.md) — Declaring a `Repository` for an entity that lives inside an aggregate, for a value object, or for read-only projectio...
- [Repository name does not match the aggregate](repository-name-does-not-match-aggregate.md) — The aggregate is called `Task`; the repository is called `TodoRepository`. Both words are fine ubiquitous language — ...
- [Resource id without an owner: a command that says which, never whose](resource-id-without-an-owner.md) — A command or query that identifies a resource belonging to somebody — `CheckoutCartCommand(cartId)`, `GetOrderQuery(o...
- [State-changing GET endpoint](state-changing-get-endpoint.md) — A `@GetMapping` (or an HTTP GET action) that executes a command use case: a "Proceed to checkout" **link** to `/check...
- [Storing raw domain events in an external (broker) outbox](storing-domain-events-in-an-external-outbox.md) — Serializing raw `DomainEvent` objects into an outbox that relays to a message broker. The tempting shortcut — "I alre...
- [Swallowed failure in a nested transaction block](swallowed-failure-in-a-nested-transaction.md) — An outer `inTransaction` block that calls another use case (or helper) which opens its own `inTransaction`, catches t...
- [Transaction boundary modelled as an output port](transaction-boundary-modelled-as-an-output-port.md) — Declaring the transaction abstraction as an output port — `interface UnitOfWork extends OutputPort` — and placing its...

**Extensible zone — authored, not generated from sources.** `pitfall/` nodes are written by a human or an LLM and **survive** `generate` (only this `index.md` is regenerated). Add one as `pitfall/<slug>.md` with frontmatter `type: Pitfall`, `title:`, `tags: [pitfall]`, then link into the skeleton with bundle-relative links (e.g. `[UseCase](/marker/port-in/usecase.md)`).
