# hexagonal

- [Classes from the domain should not access port adapters](dca-hex-001.md) — Domain should not depend on adapters (ports and adapters pattern).
- [Application Services should not access port adapters](dca-hex-002.md) — Application services should only depend on domain and outbound ports, not adapters.
- [Controllers and Resources must never access repositories directly](dca-hex-003.md) — Controllers must go through use cases (input ports), never directly to repositories.
- [Incoming Adapters must only use outbound ports (not infrastructure implementations)](dca-hex-004.md) — Incoming adapters should only use outbound ports declared as interfaces (port.out), not infrastructure implementation...
- [Outgoing adapters must not use another module's infrastructure](dca-hex-005.md) — Technical infrastructure reuse preserves module isolation.
- [Incoming port adapters must not depend directly on outgoing port adapters within the same context](dca-hex-006.md) — Port adapters should communicate through application services, not directly (event consumers are the exception).
- [Incoming adapters must only access their own bounded context (except event consumers and Open Host Services)](dca-hex-007.md) — Incoming adapters must only orchestrate use cases from their own bounded context - use integration events or the publ...
- [Classes named *Repository must reside in the outgoing adapter package (name-based discovery of unmarked repositories)](dca-hex-008.md) — Repository implementations are secondary adapters (outgoing ports).
- [Output Ports in application.shared must extend OutputPort](dca-hex-009.md) — Top-level interfaces in application.shared are output ports and must extend OutputPort to be part of the port hierarc...
- [Output ports must not reside in the domain layer](dca-hex-010.md) — output ports (Repository, Store, OutputPort) are an application-layer concern and must live in application/shared/, n...
- [Incoming Adapters must depend on input port interfaces, not on use case classes](dca-hex-011.md) — A driving adapter drives the application through its port. Injecting the concrete implementation instead couples the ...
- [Incoming Adapters must not depend on domain services](dca-hex-012.md) — An incoming adapter translates external input, calls an input port and formats its result. Injecting or invoking a do...
