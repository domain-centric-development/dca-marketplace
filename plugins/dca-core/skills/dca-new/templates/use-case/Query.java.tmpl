package {{basePackage}}.{{context}}.application.{{featureSegment}}{{usecasename}};

/**
 * Query input for the {{Name}} use case.
 *
 * <p>Queries carry the criteria for read operations. They are immutable records.
 */
public record {{Name}}Query(
{{queryFields}}
) {}
