using DomainCentric.BuildingBlocks.Ddd.Tactical;

namespace {{rootNamespace}}.{{Context}}.Domain.Model;

/// <summary>
/// Identifier of a {{Name}} — a strongly typed wrapper around a <see cref="{{idValueType}}"/>, so {{Name}}Ids
/// cannot be mixed up with other identifiers.
/// </summary>
public readonly record struct {{Name}}Id({{idValueType}} Value) : IId
{
{{idFactoryMethod}}

    public override string ToString() => Value.ToString();
}
